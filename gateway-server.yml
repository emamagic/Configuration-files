server:
  port: 7000
spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      routes:
        - id: auth-route
          uri: lb://auth-service
          predicates:
            - Path=/auth/**
          filters:
            - AddResponseHeader=X-Powered-By, Emamagic

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
    gateway:
      enabled: true


